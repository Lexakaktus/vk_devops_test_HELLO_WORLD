URL="http://127.0.0.1:5000/"              # URL для проверки
EXPECTED_STATUS=200                       # ожидаемый HTTP-код

# Пути логов и блокировок
LOGFILE="$HOME/monitor_app.log"           # файл логов
LOCKFILE="$HOME/monitor_app.lock"         # файл блокировки (flock)
RESTART_COUNTER_FILE="$HOME/monitor_app.restarts" # счётчик рестартов

# Перезапуск
SERVICE_NAME=""                           # если пусто — используется RESTART_CMD
RESTART_CMD="$HOME/vk_test/1/src/scripts/start_app.sh"  

# Поведение после рестарта
RESTART_RETRIES=3
RESTART_WAIT=5
MAX_RESTARTS_PER_HOUR=5

# curl
CURL_BIN="$(command -v curl || true)"
